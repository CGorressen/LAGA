<Window x:Class="LAGA.DatenbanksicherungEinstellungen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Datenbanksicherung Einstellungen" Height="300" Width="650"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#f0f0f0">

    <Window.Resources>
        <!-- Button-Style für konsistentes Design -->
        <Style x:Key="StandardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#c0c0c0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d0d0d0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#c0c0c0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primärer Button-Style (für wichtige Aktionen) -->
        <Style x:Key="PrimaryButtonStyle" BasedOn="{StaticResource StandardButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="#4a90e2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#357abd"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357abd"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2968a3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox-Style -->
        <Style x:Key="StandardTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#c0c0c0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- Label-Style -->
        <Style x:Key="StandardLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <!-- Titel-Bereich -->
            <RowDefinition Height="Auto"/>
            <!-- Inhalt-Bereich -->
            <RowDefinition Height="*"/>
            <!-- Button-Bereich -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Grid.Row="0" Text="Datenbanksicherung Einstellungen" 
                   FontSize="16" FontWeight="Bold" 
                   Margin="0,0,0,20" 
                   Foreground="#333333"/>

        <!-- Haupt-Inhalt -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Beschreibung -->
            <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,15"
                       FontSize="12" Foreground="#666666">
                Konfigurieren Sie den Speicherort für automatische Datenbank-Backups. 
                Backups werden bei jedem Programmstart erstellt und automatisch nach 3 Tagen gelöscht.
            </TextBlock>

            <!-- Aktueller Pfad Label -->
            <Label Grid.Row="1" Content="Backup-Speicherort:" 
                   Style="{StaticResource StandardLabelStyle}"/>

            <!-- Pfad-Eingabe und Button -->
            <Grid Grid.Row="2" Margin="0,5,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Pfad-Textfeld (read-only) -->
                <TextBox x:Name="TxtBackupPfad" Grid.Column="0"
                         Style="{StaticResource StandardTextBoxStyle}"
                         IsReadOnly="True"
                         Background="#f8f8f8"
                         Text="Wird geladen..."/>

                <!-- Pfad wählen Button -->
                <Button x:Name="BtnPfadWaehlen" Grid.Column="1"
                        Content="Pfad wählen"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Width="100"
                        Click="BtnPfadWaehlen_Click"/>
            </Grid>

            <!-- Info-Text -->
            <TextBlock Grid.Row="3" TextWrapping="Wrap" Margin="0,0,0,10"
                       FontSize="11" Foreground="#888888">
                Standard: Backups werden im LAGA-Ordner gespeichert<LineBreak/>
                Benutzerdefiniert: Backups werden in "[Gewählter Pfad]/LAGA Backup/" gespeichert
            </TextBlock>

            <!-- Status-Bereich -->
            <Border Grid.Row="4" Background="#f9f9f9" BorderBrush="#e0e0e0" 
                    BorderThickness="1" CornerRadius="3" Padding="10"
                    VerticalAlignment="Top">
                <TextBlock x:Name="TxtStatus" Text="Bereit" 
                           FontSize="11" Foreground="#666666"/>
            </Border>

        </Grid>

        <!-- Button-Bereich -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Schließen Button (links) -->
            <Button x:Name="BtnSchliessen" Grid.Column="0"
                    Content="Schließen"
                    Style="{StaticResource StandardButtonStyle}"
                    Width="100"
                    Click="BtnSchliessen_Click"/>

            <!-- Auf Standard zurücksetzen Button (rechts) -->
            <Button x:Name="BtnStandardZuruecksetzen" Grid.Column="2"
                    Content="Auf Standard zurücksetzen"
                    Style="{StaticResource StandardButtonStyle}"
                    Width="180"
                    Click="BtnStandardZuruecksetzen_Click"/>

        </Grid>

    </Grid>
</Window>